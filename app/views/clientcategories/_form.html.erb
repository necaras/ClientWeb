<%= form_for(@clientcategory) do |f| %>
  <% if @clientcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clientcategory.errors.count, "error") %> prohibited this clientcategory from being saved:</h2>

      <ul>
      <% @clientcategory.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
